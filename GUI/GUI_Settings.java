import java.awt.Color;

public class GUI_Settings {
	public colors colors = new colors();
	class colors{
		Color backround = new Color(128,0,0);
		Color button = new Color(128,0,0);
		Color menu_backround = new Color(78,1,16);
		Color backround2 = new Color(65,11,19);
		Color text = new Color(25,23,22);
}
}
